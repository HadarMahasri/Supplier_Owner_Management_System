# --- Web framework ---
fastapi>=0.110,<0.115
uvicorn[standard]>=0.27,<0.30

# --- Data validation ---
pydantic>=2.4,<3.0

# --- Database (SQLAlchemy sync) ---
SQLAlchemy>=2.0,<2.1

# --- SQL Server (ODBC) ---
pyodbc>=5.0,<6.0

# --- Env vars (.env) ---
python-dotenv>=1.0,<2.0

# --- Optional: passwords hashing (בחרי אחד) ---
# passlib[bcrypt]>=1.7,<2.0
# bcrypt>=4.1,<5.0
# argon2-cffi>=23.1,<24.0
